require 'test_helper'

class ApplicationHelperTest < ActionView::TestCase
  test 'root or admin url only redirects in production' do
  end

  test 'root or admin path only redirects in production' do
  end

  test 'root or admin url only redirects if root redirect enabled' do
  end

  test 'root or admin path only redirects of root redirect enabled' do
  end

  test 'root or admin url redirects to right target' do
  end

  test 'root or admin path redirects to right target' do
  end
end
