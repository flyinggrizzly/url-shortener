<%= provide(:title, 'Find a Short URL') %>

<div class="column">
  
  <%= render 'short_urls/controls' %>

  <div class="search_results">
    <% if @short_urls.present? %>
      <ul class="short_urls">
        <%= render @short_urls %>
      </ul>
    <% else %>
      <p>No short URLs matching your search were found.</p>
    <% end %>
  </div>
</div>
